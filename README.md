# ProdSight
Single-file offline HTML app for monitoring factory production plans and outputs from Excel, with Jalali dates, RTL Persian UI, robust data parsing, and auto-refresh — fully configurable via .env.
